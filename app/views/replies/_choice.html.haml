= c.label :value, c.object.question.title
- c.object.question.possible_answers.each do |answer|
  .radio
    %label
      = answer.title
      = c.radio_button :possible_answer_id, answer.id
      = c.hidden_field :question_id
