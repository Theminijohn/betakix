%h1 Listing polls

%table
  %tr
    %th Title
    %th
    %th
    %th
    %th

  %ul
    - @polls.each do |poll|
      %li
        %p= link_to poll.title, poll
        %p.btn-group.btn-group-xs
          = link_to 'Edit', edit_poll_path(poll), class: 'btn btn-default'
          = link_to 'Add Question', new_poll_question_path(poll), class: 'btn btn-default'
          = link_to 'Delete', poll, method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-danger'

%br

= link_to 'New Poll', new_poll_path, class: 'btn btn-primary'
